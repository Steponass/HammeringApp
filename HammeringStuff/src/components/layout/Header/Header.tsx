import styles from './Header.module.css'

export default function Header() {
  return (
    <header className={styles.header} >
      <h4>to someone with a hammer, everything is a nail</h4>
    </header>
  );
}
